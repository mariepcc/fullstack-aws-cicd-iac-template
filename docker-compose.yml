services:
  backend:
    image: ${ECR_REGISTRY}/${ECR_BACKEND}:latest
    ports:
      - "8000:8000" 
    restart: always

  frontend:
    image: ${ECR_REGISTRY}/${ECR_FRONTEND}:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always